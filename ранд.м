модуль РАНД

  ;; спосіб знайти остачу від числа, поки це не імплементовано через %, потім змінити на %
  дія остача(а число, б число) число
    а-global.Math.trunc(а/б)*б
  кінець

  ;; спосіб знайти довжину списку, поки це не імплементовано через список.довжина(), потім оновити
  дія довжина_списку(список Список) число
    довжина = 0
    перебрати список як елемент
      довжина = довжина + 1
    кінець
    довжина
  кінець

  ;; повертає випадкове число між 0 та 1
  дія число() число
    global.Math.random()
  кінець

  ;; повертає випадкове ціле число у даному проміжку від мінімального значення (включно) до максимального (включно)
  дія проміжок(мін число, макс число) число
    макс = макс + 1
    різниця = макс-мін
    global.Math.trunc(остача(число()*різниця, різниця) + мін)
  кінець

  ;; повертає випадковий елемент списку
  дія елемент(список Список)
    список(проміжок(0, довжина_списку(список)-1))
  кінець

  дати число
  дати проміжок
  дати елемент
кінець
